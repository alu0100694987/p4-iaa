/**
 * @Author:
 * Fecha: 27/02/2014
 * E-mail:
 * Descripción: Clase Regla - Representa a cada una de las reglas del embalse,
 *              dada por un antecedente y un consecuente.
 *         
 */
public class Regla {
  
  private IError antecedente;
  private IApertura consecuente;
  
  /*double Funcion_error_negativo(double x){
    if((-15<=x) && (x<=-10))
      return 1.0;
    else if((-10<=x)&&(x<=0))
      return ((-x)/10);
    else 
      return 0;
  }
  
  double Funcion_error_positivo(double x){
    if((0<=x) && (x<=10))
      return (x/10);
    else if((10<=x)&&(x<=15))
      return 1.0;
    else 
      return 0;
  }
  
  double Funcion_error_cero (double x){
    if((-5<=x) && (x<=0))
      return ((5+x)/5);
    else if((0<=x)&&(x<=5))
      return ((5-x)/5);
    else 
      return 0;
  }
  
  double Funcion_apertura_abrir(double x){
    if((0<=x) && (x<=50))
      return (x/50);
    else if(50<=x)
      return 1.0;
    else 
      return 0;
  }
  
  double Funcion_apertura_cerrar(double x){
    if((-25<=x) && (x<=0))
      return ((25+x)/25);
    else if((0<=x) && (x<=25))
      return((25-x)/25);
    else 
      return 0;
  }*/
  
}
